<ul id="side-menu">
  {{# if menu}}
  {{# each menu as |key value|}}
  <a href="{{key.selectedURL}}"><li id="{{key._id}}">{{key.navName}}</li></a>
  {{/each}}
  {{else}}
  {{>admin/sidenav}}
  {{/if}}
</ul>