export default (function fixEnter () {
  const editor = document.querySelector('.editContent')
  // editor.addEventListener('keypress', e => console.log(e))
  // editor.addEventListener('keydown', e => console.log(e))
  // editor.addEventListener('keyup', e => console.log(e))
  // console.log('fix enter')
})()